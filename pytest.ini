[pytest]
testpaths = tests
asyncio_mode = auto
norecursedirs = .git .venv venv env .tox build dist .cache __pycache__ site-packages
addopts = -q
filterwarnings =
    ignore:.*on_event is deprecated.*:DeprecationWarning